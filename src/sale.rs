use nature_common::{ConverterParameter, ConverterReturned};

#[no_mangle]
#[allow(unused_attributes)]
#[allow(improper_ctypes)]
pub extern fn order_to_item(_para: &ConverterParameter) -> ConverterReturned {
    // load order data
    // put to counter
    // make instances
    unimplemented!()
}
